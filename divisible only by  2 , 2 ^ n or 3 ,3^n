a=[]
for i in range(1,101):
	b=[]
	c=0
	for j in range(1,(i+1)):
		if i%j==0:
			b.append(j)
	print b
	
	for k in b:
		if k==1 or k%2==0 or k%3==0:
			continue
		else:
			c=c+1
	if c==0:
		a.append(i)


print "answer"
print a
